package de.fida.cwtool.util;

import java.util.*;

public class ListComperator {
   /* public static boolean equalLists(List<Object> one, List<Object> two){
        if (one == null && two == null){
            return true;
        }

        if((one == null && two != null)
                || one != null && two == null
                || one.size() != two.size()){
            return false;
        }

        //to avoid messing the order of the lists we will use a copy
        //as noted in comments by A. R. S.
        one = new ArrayList<Object>(one);
        two = new ArrayList<Object>(two);

        Collections.sort(one);
        Collections.sort(two);
        return one.equals(two);
    }*/
}
